% Single iteration of updating the weight% This function takes 4  arguments% This function outputs the final weight of that iterationfunction [res] = update_weight_once(input,weight,output,ln)   [row,col] = size(input); % Finding number of rows and columns in input   inc = 1;   while inc <= row %couts upto number of rows in the input file       weight = nn(input(inc,:),weight,output,ln); %calls the core neural net function     inc = inc + 1;   end   res = weight; % returns the final weightend 